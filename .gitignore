# Terraform
**/.terraform/
**/*.tfstate
**/*.tfstate.*
**/*.tfvars
!**/*.tfvars.example
**/.terraform.lock.hcl

# kind / kube
**/kubeconfig

# secrets / local keys (never commit)
**/*secret*
**/*.pem

# local-only migration notes
docs/STATE.md
docs/local-sftp-airbyte-overview.md

# editor
*.swp
.idea/
.vscode/
