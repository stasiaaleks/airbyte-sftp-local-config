# Terraform
**/.terraform/
**/*.tfstate
**/*.tfstate.*
**/*.tfvars
!**/*.tfvars.example
**/.terraform.lock.hcl

# kind / kube
**/kubeconfig

# secrets / local keys (never commit)
**/*secret*
**/*.pem

# editor
*.swp
.idea/
.vscode/
